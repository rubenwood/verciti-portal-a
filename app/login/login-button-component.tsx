"use client"

export default function LoginButton() {
    const handleLogin = () => {

    };

    return (
        <button className="button" onClick={handleLogin}>
            Login
        </button>
    );
}